export interface Seva {
  seva_name: string
  amount: number
  is_default: string
  display_order: number
  is_recurring: string
  recurring_interval: string
  recurring_count: number
  remarks: string
  required: boolean   
}
